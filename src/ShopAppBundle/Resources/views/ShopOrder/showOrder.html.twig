{% extends '::base.html.twig' %}
{#{{ dump(entity) }}#}
{% block body -%}
    <h1>Zamówienie</h1>
    {% if entity is not defined %}
        Brak zamówień!
        {% endif %}
    <table class="record_properties">
        
        <tbody>
            <tr>
                <th>Numer zamówienia</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ entity.status }}</td>
            </tr>
            <tr>
                <th>Ilość</th>
                <td>{{ entity.quantity }}</td>
            </tr>
        </tbody>
        
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('user_show') }}">
            Wróć do swojego profilu
        </a>
    </li>
    {#<li>
        <a href="{{ path('shoporder_edit', { 'id': order.id }) }}">
            Edit
        </a>
    </li>#}
{#    <li>{{ form(delete_form) }}</li>
#}</ul>
{% endblock %}